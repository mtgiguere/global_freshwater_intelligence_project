# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.pdb
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# Testing & coverage
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Ruff
.ruff_cache/

# Jupyter
.ipynb_checkpoints/
*.ipynb~

# Data — raw files are immutable and stored in S3/DVC, not git
data/raw/
data/interim/
data/processed/
# Trained model files — generated by src/models/train_all.py, not committed
# (*.joblib is also excluded globally below, but the directory is listed
# explicitly so the whole folder is clearly understood as generated output)
data/models/

# External shapefiles and lookup tables can be large — add individually if small
# data/external/

# ML artefacts
mlruns/
*.pkl
*.joblib
*.h5
*.pt
*.pth
models/weights/

# Environment
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo

# R
# .Rprofile is excluded -- it sources renv/activate.R which interferes with CI
# package installation until setup-renv@v2 is fully configured.
analysis/.Rprofile
analysis/renv/library/
analysis/renv/staging/
analysis/renv/sandbox/
.Rhistory
.RData
.Rdata
.httr-oauth
*.Rproj.user/

# OS
.DS_Store
Thumbs.db
