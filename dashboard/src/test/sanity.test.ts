// Vitest infrastructure sanity check — confirms the test runner is wired up correctly.
// This file can be deleted once a real test exists in the suite.
describe('vitest setup', () => {
  it('runs a test', () => {
    expect(1 + 1).toBe(2)
  })
})
